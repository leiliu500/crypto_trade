The size-aware historical training path now has no unresolved critical issue found in this bounded review. It prepares and imports counterfactual training labels; it does not establish profitable trading or create prospective validation evidence.

The review covered `risk-training-context.ts`, `risk-training-source.ts`, `training-backfill.ts`, `training-import.ts`, `training-main.ts`, `replay.ts`, the relevant controller and efficient-trainer restore paths, and engine/startup wiring. No owned implementation files were edited by this reviewer and no historical strategy performance or reserved 2026 performance was run.

Two source compatibility gaps were found and corrected by the implementation owner before this conclusion:

- The first importer only checked the internal consistency of source-hash metadata. It now compares the artifact with trusted canonical TypeScript source hashes supplied by startup. The source helper traverses 81 files, including the previously omitted book walk, market primitives, and fee validation dependencies. The preparation command checks this identity again before publication.
- A resumed artifact could previously re-stamp labels from older execution code with the new run's source identity. Resume now requires matching prior version, costs, assets, feature/sizing context, distribution and training specifications, source identity, and origin audit hash. Existing receipt and training clocks remain preserved.

The reviewed causal and accounting contracts are intact:

- Accepted books update the same base features, book pressure, and deterministic extensions as the live engine. Trades advance those features using the unchanged provider quote, with the trade receive time, and do not become new quote decisions. Raw recorder order is retained; streams are not sorted to manufacture a different information sequence.
- Completed labels are available only at or after their completion. Future input rows are excluded before replay. Resumed labels cannot preload earlier events, and scheduler receipt floors also reject a prefix earlier than the previous artifact's final observation even when its last label completed sooner.
- The original fixed-$12 sizing identity cannot enter this bank. Each retained action uses the current sizing-policy identity and has an origin containing its real recorded book, feature context, requested quantity, and reference capital. Import recomputes quantity from that origin under the trusted policy, costs, and instrument rules. Prices, fees, and depth-dependent fills are recomputed by the shared execution path during preparation; previous labels are not multiplied into a different size.
- Historical data supplies independent complete action labels for the efficient, non-regime profile. Import clears pending and completed prospective selections whenever it changes the effective training bank. An idempotent repeated import leaves subsequently collected validation intact. Import runs before engine startup and does not submit orders or reconcile a fictitious account balance.
- Recorded inputs are hashed while streamed, checked for filesystem changes, and written to a new artifact with atomic no-overwrite publication. Startup rejects aliases to files it will later mutate and preserves the prior live bank on failed import.

Verification: `node --import tsx --test test/distribution-risk-training.test.ts test/distribution-efficient-import.test.ts test/distribution-training-backfill.test.ts` passed after both source-identity fixes. The independent synthetic receipt-clock probe is retained as `risk-training-resume-audit.mjs` and `risk-training-resume-audit.json`; all three boundary cases passed. These synthetic fixtures test behavior and contain no historical strategy outcomes.

Limits relevant to profit claims: the declared reference capital is counterfactual, not reconstructed historical account equity. Labels include execution fees and the existing fixed reserve, and do not include observed funding cash. Startup wall-clock delay and actual broker fills are not reproduced by the recorder's receive-time path. Source compatibility and artifact integrity do not substitute for independently executing the raw-data preparation or for prospective funded net profit. The existing sample-support, statistical, validation, liquidity, account, and risk gates remain necessary.
