The raw recordings can support a fresh size-matched replay, but the existing historical trainer and importer cannot build or load the v14 bank. The trainer constructs a legacy fixed-$12 controller; the importer explicitly rejects risk-sized states. A separate versioned path is being implemented, preserving the existing gates and rejecting legacy-size labels.

At the read-only inspection, 34 frozen concatenated-gzip archives contain 7,114,338,384 compressed bytes (6.626 GiB). Their validated first/final gzip-member public timestamps span 2026-09-04T22:15:59.790Z through 2026-09-09T20:16:36.489Z, covering six UTC dates. Exact per-file endpoints are in the JSON. This boundary scan read bounded archive heads and tails; it does not claim uninterrupted usable book coverage. The earlier full-path legacy audit independently showed valid paths on September 5, 6 and 7, but those old-size labels cannot be reused. All v14 quantities and paths must be simulated anew.

The active v14 bank has 194 completed labels, all from September 9; action banks contain only 2–37 labels. The 197 most recent dashboard decisions all report INSUFFICIENT_DAYS. Current supported nonzero mean estimates are negative, so removing the historical support deficit cannot promise an entry or profit. At the sampled snapshot, both symbols also had LIQUIDITY_BELOW_MINIMUM_ORDER from the existing 1% top-level participation rule. The effective notional cap was $999.9828269740003, so the former $12 cap is not this snapshot’s blocker.

The minimum is three materially weighted UTC dates in the queried action’s feature neighborhood, not three calendar dates anywhere in an archive. The current model also requires at least 48 local samples, 32 effective samples, recent support, and a positive conservative score after fees and reserve. All remain unchanged.

The same-policy label simulator applies recorded books/trades, actual simulated fills, five-bp taker fees, a three-bp fixed reserve and its three declared execution stresses. It does not directly incorporate observed hourly funding cash in these labels. Adding a new funding payoff definition would require a separately versioned label/cost contract; the running paper funding cash ledger is distinct.

A size-aware replay must reproduce FeatureEngine and DeterministicFeatureExtensions updates on both accepted books and trades, validated L2 flow, and a declared reference equity/high-water context. It must preserve source emission order, per-stream reversal checks, gaps, stale-quote rejection, nonfills, partial fills and independent 5/15/30-minute scheduling. Archive labels are counterfactual execution outcomes, not real fills or prospective validation.

There is no truthful executable v14 rebuild command until that new CLI is implemented. A full replay scans all 6.626 GiB compressed data plus L2 reconstruction and feature updates; budget tens of minutes to hours pending a measured benchmark. The bounded endpoint inspection completed in under a second after Docker access. No bank was imported, activated or overwritten during this feasibility audit.
